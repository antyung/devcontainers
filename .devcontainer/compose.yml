---
services:
  devcontainer-devcontainers:
    container_name: devcontainer-devcontainers
    build:
      context: .
    volumes:
      - ..:/home/vscode/devcontainers:cached
    user: vscode
    tty: true
