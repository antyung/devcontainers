---
services:
  devcontainer-python-base:
    container_name: devcontainer-python-base
    build:
      context: .
    volumes:
      - ..:/home/vscode/python-base:cached
    user: vscode
    tty: true
